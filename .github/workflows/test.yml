name: test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/test-matrix.yml

  test:
    uses: ./.github/workflows/test-matrix.yml
